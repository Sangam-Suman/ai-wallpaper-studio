package com.example.aiwallpaper.data.model

enum class ImageProvider(val label: String, val subtitle: String) {
    GEMINI("Google AI", "Requires API key"),
    POLLINATIONS("Free FLUX", "No key needed")
}